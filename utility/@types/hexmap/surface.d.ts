interface HmSurfaceState {
	isPainted: boolean;
	isHovered: boolean;
}

interface HmPoints {
	topLeft: HmPoint;
	topRight: HmPoint;
	right: HmPoint;
	bottomRight: HmPoint;
	bottomLeft: HmPoint;
	left: HmPoint;
}
